apiVersion: v1
kind: Service
metadata:
  name: first-poc-composer-svc
  annotations:
    external-dns.alpha.kubernetes.io/hostname: first-poc-composer.hmg.nexteldigital.com.br
    service.beta.kubernetes.io/aws-load-balancer-internal: 0.0.0.0/0
  labels:
    app: first-poc-composer
spec:
  selector:
    app: first-poc-composer
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
